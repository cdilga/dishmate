# CLAUDE.md

**Read `AGENTS.md` first** — it contains all shared project instructions (architecture, commands, patterns, guardrails). This file only has Claude Code-specific additions.

## Claude-specific

- **Frontend work requires `/frontend-designer`** — whenever touching frontend code (HTML, CSS, UI rendering, `demo/main.ts`, `demo/index.html`), always invoke the `/frontend-designer` skill first. See `specs/PROJECT_GUARDRAILS.md`.
